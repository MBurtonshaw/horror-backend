# Local Configuration (commented out for now)
# spring.web.cors.allow-credentials=true
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.datasource.url=jdbc:postgresql://localhost:5432/HorrorFilms
# spring.datasource.username=postgres
# spring.datasource.password=postgres1

# Deployment Configuration (Heroku)
spring.datasource.url=${JDBC_DATABASE_URL}

# CORS settings for frontend
spring.web.cors.allow-credentials=true
spring.web.cors.allowed-origins=https://horror-catalogue.vercel.app
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

server.port=${PORT:8080}